# Clash RS 桌面版完整功能实施计划

> 状态：**已实施；macOS 已构建验证，Windows 等待 Windows 主机产物验证**
>
> 适用平台：macOS、Windows
>
> UI 参考截图仅保存在本地，不纳入版本库。
> 实施记录：共享 Flutter 桌面 UI、mihomo 核心与两端网络适配已落地。

## 1. 项目目标

在保留 Android 现有功能和Android Rust 核心的前提下，完成可独立分发的 Clash RS 桌面客户端：

- macOS 与 Windows 使用同一套 Flutter 桌面界面和业务控制器。
- 还原参考图中的首页、代理、订阅、连接、规则、日志、测试和设置页面。
- 两个平台均具备系统代理、TUN/虚拟网卡、配置管理、节点管理、运行状态和故障恢复能力。
- 平台相关能力封装在独立适配层，避免桌面实现影响 Android。
- 所有系统状态变更均可恢复；异常退出后可检测并修复残留代理、进程和路由。

## 2. 已确认的桌面窗口规范

| 项目 | macOS | Windows |
|---|---|---|
| 内容区固定尺寸 | `960 × 720` | `960 × 720` |
| 调整大小 | 关闭 | 关闭 |
| 最大化 | 关闭 | 不显示最大化按钮 |
| 最小化 | 保留 | 保留 |
| 默认位置 | 最大可用屏幕居中 | 当前屏幕居中 |
| 桌面布局 | 220px 紧凑侧栏 + 双列内容 | 与 macOS 共用 |

窗口缩小后，所有页面必须通过内部滚动完成访问，不允许出现 RenderFlex 溢出、横向截断或不可点击区域。

## 3. 参考页面拆解

| 参考图 | 页面/模块 | 计划实现内容 |
|---|---|---|
| `mac-1001` | 首页总览 | 订阅、当前节点、网络设置、代理模式、流量入口 |
| `mac-1002` | 首页流量 | 实时折线、上下行速度、累计流量、连接数、网络测试、IP 信息 |
| `mac-1003` | 首页诊断 | 常用网站测试、Clash 信息、系统信息、运行时间、版本与更新状态 |
| `mac-1004`、`mac-1012` | 代理 | 分组标签、节点卡片、协议/UDP 标签、延迟、选择与批量测试 |
| `mac-1005` | 订阅 | URL 导入、刷新、激活、流量/到期信息、Merge/Script 扩展入口 |
| `mac-1006` | 连接 | 实时连接表、上传/下载、链路、筛选、关闭单条/全部连接 |
| `mac-1007` | 规则 | 规则序号、类型、载荷、策略、关键词过滤与命中信息 |
| `mac-1008` | 日志 | 级别过滤、文本过滤、暂停、自动滚动、清空与导出 |
| `mac-1009` | 测试 | 常用服务连通性、延迟结果、单项/全部测试与结果状态 |
| `mac-1010` | 设置 | 系统、Clash、界面、语言、主题、端口、启动和更新设置 |
| `mac-1011` | 高级设置 | 端口、外部控制、GeoData、内核目录、备份恢复、开发者工具 |

## 4. 总体架构

```text
Flutter Desktop UI
  ├── DesktopShell / Navigation / Responsive Scroll
  ├── ProxyAppController（共享业务状态）
  ├── DesktopRuntimeController（桌面运行状态）
  └── PlatformNetworkService
       ├── MacNetworkService
       │    ├── networksetup 系统代理
       │    └── 管理员助手 + TUN
       └── WindowsNetworkService
            ├── WinHTTP/Internet Settings 系统代理
            └── 管理员助手 + Wintun

Desktop mihomo Core
  ├── 普通模式独立进程
  ├── helper / Wintun TUN 模式
  └── 9090 REST Controller：proxies / traffic / connections / rules / logs
```

### 4.1 共享层

- 配置与订阅模型、存储仓库、更新调度。
- 节点分组、选择、测速、代理模式和运行状态。
- 流量、连接、规则、日志、诊断的控制器接口。
- 桌面设置模型及版本迁移。

### 4.2 平台层

- macOS：AppKit、`networksetup`、管理员辅助进程、TUN、菜单栏。
- Windows：Win32 Runner、注册表/WinINet 系统代理、Wintun、托盘菜单。
- 两个平台保存修改前系统快照，并在停止、切换模式、异常恢复时还原。

## 5. 分阶段实施

### 阶段 A：桌面基础与运行状态（P0）

1. 固定窗口、紧凑侧栏、标题栏、滚动和高 DPI 适配。
2. 建立共享桌面入口，macOS/Windows 均进入 `DesktopApp`。
3. 整理控制器生命周期：初始化、启动、停止、重启、异常恢复。
4. 增加单实例、托盘/菜单栏、关闭驻留与真正退出流程。

**验收**：两端固定 `960 × 720`；八个页面可进入；窗口内无溢出；应用能安全启动和停止核心。

### 阶段 B：配置、订阅与代理（P0）

1. URL/文件导入、配置校验、保存、激活、删除和刷新。
2. 订阅用量、到期时间、更新时间和自动更新。
3. 代理分组、节点选择、单节点/分组/全部延迟测试。
4. Rule/Global/Direct 模式实时切换并持久化。

**验收**：重启后保留激活配置、代理模式和节点选择；刷新失败不破坏旧配置。

### 阶段 C：系统代理与全局接管（P0）

1. macOS 系统代理的 HTTP/HTTPS/SOCKS 快照、设置和恢复。
2. macOS 管理员助手、TUN、自动路由、DNS 劫持和退出清理。
3. Windows 系统代理适配器及变更通知。
4. Windows 管理员助手、Wintun 安装/检测、路由与 DNS 接管。
5. 模式互斥：系统代理、虚拟网卡、关闭三种状态原子切换。

**验收**：两端代理后出口变化；关闭后原网络设置恢复；异常退出重启可自动修复残留状态。

### 阶段 D：首页仪表盘（P1）

1. 当前订阅、当前节点、延迟、协议和出口信息。
2. 实时上下行曲线、速度、累计流量、连接数和内存占用。
3. 常用网站检测、网络延迟、出口 IP 与 ASN/地区信息。
4. 核心、系统、版本、运行时间和更新时间信息。

**验收**：数据来自真实运行接口；停止状态清零；长时间运行不持续增长定时器或订阅。

### 阶段 E：连接、规则与日志（P1）

1. 连接列表实时刷新、排序、过滤、关闭单条/全部连接。
2. 规则列表过滤、类型/载荷/策略展示及命中规则定位。
3. 日志流、级别过滤、暂停、清空、自动滚动和文件导出。

**验收**：高连接量下界面保持响应；筛选和关闭操作实时反馈；日志有上限且可导出。

### 阶段 F：测试与设置（P1）

1. 常用服务连通性和延迟测试，支持单项、全部和取消。
2. 系统设置、启动行为、托盘、语言、主题和通知。
3. Clash 端口、DNS、IPv6、GeoData、外部控制和覆写设置。
4. 设置合法性校验、变更提示、按需重启核心和恢复默认值。

**验收**：设置修改后行为与展示一致；无效端口、冲突端口和缺失文件有明确错误信息。

### 阶段 G：分发与质量门禁（P0）

1. macOS Intel/Apple Silicon 通用 App、签名、DMG 和校验文件。
2. Windows x64 构建、运行库、安装包/便携包和校验文件。
3. 单元、Widget、平台适配、核心运行和安装升级测试。
4. README、用户帮助、故障排查、卸载与系统恢复说明。

**验收**：新机器完成安装、导入、启动、代理、停止、恢复和卸载闭环。

## 6. 数据与状态设计

- `DesktopSettings`：窗口、托盘、启动、主题、语言、更新行为。
- `NetworkSnapshot`：平台网络服务、代理端点、DNS、路由及恢复状态。
- `RuntimeState`：核心 PID、模式、控制端口、启动时间和错误状态。
- `TrafficSnapshot`：上传、下载、累计、连接数和采样时间。
- 敏感字段只保存在本机应用数据目录，日志展示时脱敏。

## 7. 测试矩阵

| 类别 | macOS | Windows |
|---|---|---|
| 固定窗口/高 DPI/多屏 | 必测 | 必测 |
| 配置导入和订阅刷新 | 必测 | 必测 |
| 节点选择和延迟测试 | 必测 | 必测 |
| 系统代理设置与恢复 | 必测 | 必测 |
| TUN/Wintun 全局接管 | 必测 | 必测 |
| 睡眠、唤醒、切网 | 必测 | 必测 |
| 异常退出恢复 | 必测 | 必测 |
| 安装、升级、卸载 | DMG | 安装包/便携包 |

## 8. 完成定义

- `TODO.md` 中 P0、P1 项全部完成并有验证记录。
- 参考图覆盖的页面和交互均可用，不保留静态占位数据。
- macOS 与 Windows 的系统代理及 TUN/Wintun 均完成真机闭环。
- 自动化测试通过，发布包架构、签名/校验和安装流程通过。
- README、帮助、故障恢复和分发说明同步更新。
